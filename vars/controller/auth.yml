---
nolog_debug: false

aap_hostname: "aap25-ocp-ansible-automation-platform.apps.ocp.bontreger.dev"
aap_username: "{{ vault_controller_username }}"
aap_password: "{{ vault_controller_password }}"
aap_validate_certs: false
aap_request_timeout: 60

proxmox_hostname: "{{ vault_proxmox_hostname }}"
proxmox_username: "{{ vault_proxmox_username }}"
proxmox_password: "{{ vault_proxmox_password }}"